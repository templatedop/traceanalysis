global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files: []

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'observability-analysis'
    static_configs:
      - targets: ['server:8080']
    metrics_path: '/metrics'

  - job_name: 'observability-analysis-worker'
    static_configs:
      - targets: ['worker:8080']
    metrics_path: '/metrics'
